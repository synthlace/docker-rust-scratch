[package]
name = "docker-rust-scratch"
version = "0.1.0"
edition = "2021"

[profile.release]
strip = true
lto = true
panic = 'abort'

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rocket = "0.5.0"
